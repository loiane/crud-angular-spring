{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "test-coverage",
            "type": "shell",
            "command": "mvn jacoco:prepare-agent test install jacoco:report",
            "group": "test"
        }
    ]
}